spring.application.name=servicio-productos
server.port=${PORT:0}
eureka.instance.instance-id=${spring.application.name}:${spring.application.instance_id:${random.int(5000,5100)}}
eureka.client.service-url.defaultZone=http://localhost:8761/eureka